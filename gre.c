#include<stdio.h>
int main()
{
int a,b;
printf("Enter two different number\t");
scanf("%d%d",&a,&b);
if (a>b)
printf("\nGreater number is =\t%d",a);
else
printf("\nGreater number is =\t%d",b);
return 0;
}	
